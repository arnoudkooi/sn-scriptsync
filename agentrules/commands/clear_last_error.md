### `clear_last_error`
Clear the last error file.

**Request:**
```json
{ "id": "clr", "command": "clear_last_error" }
```

**Response:**
```json
{
  "result": {
    "cleared": true,
    "message": "Error cleared"
  }
}
```

